import { CustomDateFormatPipePipe } from './custom-date-format-pipe.pipe';

describe('CustomDateFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
